# Local configuration and secrets
config.local.php
.env
.env.*
!.env.example

# Private data supplied by the client must never be versioned or deployed.
_IMPORTACION_PRIVADA/

# Local visual reference only; it is not an asset licensed for this project.
imagenes/ejemplochat.png

# Local tooling
.idea/
.vscode/
*.log
